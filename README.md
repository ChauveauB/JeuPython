# Jeu Python

### Installation

Pour installer le jeu, il faut lancer la commande suivante:
    
```bash
pip3 install -r requirements.txt
```

### Lancer le jeu

Pour lancer le jeu, il faut lancer la commande suivante depuis le dossier src:
    
```bash
python3 main.py
```